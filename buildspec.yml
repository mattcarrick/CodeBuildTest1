version: 0.2

phases:
  build:
    commands:
      - touch test.txt
      - mkdir artifacts
      - failedcommand
      - mv test.txt artifacts/
  post_build:
    commands:
      - echo This still gets done
      - touch artifacts/output.txt
      
artifacts:
  files:
    - 'artifacts/*'
